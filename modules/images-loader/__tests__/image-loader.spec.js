import ImagesLoader from '../src/main'

describe('ImagesLoader', () => {
  describe('ImagesLoader()', () => {
    it('should have ImagesLoader', () => {
      expect(ImagesLoader).toBeFunction()
    })
  })
})
