import Gradient from '../../src/main'
import '@pluginjs/color'

describe('Gradient', () => {
  describe('Gradient()', () => {
    it('should have Gradient', () => {
      expect(Gradient).to.be.an('function')
    })
  })
})
